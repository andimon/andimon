---
layout: null
---
[
  {% for project in site.projects %}
  {
    "title": {{ project.title | jsonify }},
    "git_url": {{ project.git_url | default: "" | jsonify }},
    "excerpt": {{ project.excerpt | strip_html | strip | jsonify }}
  }{% unless forloop.last %},{% endunless %}
  {% endfor %}
]
